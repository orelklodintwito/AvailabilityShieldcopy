# Demo plan

Start the stack, show healthy status, generate normal traffic, run a direct flood for degradation, reset, run the shielded flood, show queue/decisions/events, inspect Layer 4 metrics, export a report, and finish with `npm run validate:all`.
