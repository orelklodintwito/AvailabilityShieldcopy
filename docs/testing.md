# Testing

Run `npm test`, `npm run lint`, and `npm run build` for frontend/unit checks. With the protected app and gateway running, `npm run validate:all` additionally checks Layer 4 policy, API envelopes, invalid limits, simulation allow-listing, queue telemetry, health, heavy endpoint mitigation, direct-vs-shielded flood behavior, queue processing, and basic endpoint preservation.
