# Testing

Run `npm run build` for the frontend bundle and `npm run validate:backend` with the protected app and gateway running. Validation covers health, heavy endpoint mitigation, direct-vs-shielded flood behavior, queue processing, and basic endpoint preservation.
